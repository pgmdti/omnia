{% extends "base.html.twig" %}
{% block header %}
{% endblock %}

{% block content %}
    <div class="fundo-topo-menu">
        Cadastro de Ato
    </div>

    <div class="container-fluid">
        <div class="row">
            <div class="container">
                <div class="col-md-11 col-xs-12">
                    {{ form_start(form, {'attr': {'id': 'form_act_new'}}) }}
                    <div class="col-md-3 col-xs-12">
                        {{ form_row(form.emissao, {'label':'Data'}) }}
                    </div>
                    <div class="col-md-4 col-xs-12">
                        {{ form_row(form.numero, {'label':'Nº do Processo'}) }}
                    </div>
                    <div class="col-md-5 col-xs-12">
                        {{ form_row(form.tipodeato, {'label':'Tipo de Ato'}) }}
                    </div>
                    <div class="row"></div>
                    <br>
                    <div class="col-md-5 col-xs-12">
                        {{ form_row(form.procurador, {'label':'Procurador'}) }}
                    </div>
                    <div class="col-md-7 col-xs-12">
                        {{ form_row(form.descricao, {'label':'Descrição'}) }}
                    </div>
                    <div class="row"></div>
                    <br>
                    <div class="col-md-12 col-xs-12">
                        <input class="btn btn-primary mySpinner" type="submit" value="Enviar" />
                    </div>
                    <br>
                    <input type="hidden" id="url-action-form" value="{{ path("esp_produtividade_analista_ato_novo") }}">
                    <input type="hidden" id="save-action-form" name="_save-action-form" value="0">
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>

{% endblock %}